# tox -c tox-local.ini
[tox]
envlist = py36,py38,py39

[testenv]
deps=
    -rrequirements.txt
    pytest
    coverage
    pytest-cov
setenv =
    PYTHONPATH={toxinidir}
recreate = True
commands=pytest -v --cov policyhandler --cov-report html


